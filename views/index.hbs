<main>
    <div>
        <h2><a href="/play.html">New Game</a></h2>
        <div>
            <h3>Wie funktioniert das Spiel?</h3>
            <ol>
                <li>Sie bekommen einen zufälligen Straßennamen angezeigt.</li>
                <li>Sie wählen auf der Karte Ihre Vermutung aus.</li>
                <li>Je nach Genauigkeit bekommen Sie Punkte zugeteilt.</li>
            </ol>
        </div>
    </div>
    <div>
        <h2>High scores</h2>
        <div>
            {{#each games}}
                <div><a href="/game.html?id={{ id }}">{{ username }}{{#if me}} (ich){{/if}}</a>: {{ points }} Punkte</div>
            {{/each}}
        </div>
    </div>
    <div>
        <h2>Most viewed</h2>
        <div>
            {{#each views}}
                <div><a href="/game.html?id={{ id }}">{{ username }}{{#if me}} (ich){{/if}}</a>: {{ points }} Punkte (Views: {{ count }})</div>
            {{/each}}
        </div>
    </div>
    <div>
        <h2>Favorites</h2>
        <div>
            {{#each favorites}}
                <div><a href="/game.html?id={{ id }}">{{ username }}{{#if me}} (ich){{/if}}</a>: {{ points }} Punkte</div>
            {{/each}}
        </div>
    </div>
</main>
